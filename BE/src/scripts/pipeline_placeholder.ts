/**
 * Placeholder entry for npm run pipeline / npm run alert.
 * Fetch: npm run fetch-cron / fetch-cron:now
 * Analysis: npm run analysis-cron / analysis-cron:now
 */
console.log(
  "[ourcrowd-be] Use `npm run fetch-cron` and `npm run analysis-cron` (or `:now` variants) for the mention pipeline.",
);
