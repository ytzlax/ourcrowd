/**
 * Placeholder entry for npm run pipeline / npm run alert.
 * Data collection: npm run cron:now
 * Scheduled alerts: npm run cron
 */
console.log(
  "[ourcrowd-be] Use `npm run cron:now` for the mention pipeline and `npm run cron` for the daily schedule.",
);
